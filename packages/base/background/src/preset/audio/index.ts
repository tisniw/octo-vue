/**
 * audio 分类预设桶
 *
 * DESIGN.md §14.6，P3 阶段交付。
 *
 * 计划包含 4 个音频驱动可视化预设，每个独立目录：
 *   - spectrum/            → 'audio-spectrum'            通用
 *   - waveform/            → 'audio-waveform'            通用
 *   - particles-beat/      → 'audio-particles-beat'      通用
 *   - visualizer-circle/   → 'audio-visualizer-circle'   通用
 *
 * 音频背景全部为通用，不绑定特定视觉。
 *
 * 当前阶段：仅占位 barrel。
 */

export type {} from '../../core'